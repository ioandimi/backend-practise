generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Book {
  id        String  @id @default(cuid())
  title     String
  author    String
  summary   String
  pages     Int
  published Boolean @default(false)
}
